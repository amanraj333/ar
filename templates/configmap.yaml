# apiVersion: v1
# kind: ConfigMap

# metadata:
#   name:  #myntra-config

# data:
#   # APP_NAME: myntra-clone
#   # ENV: production
#   # PORT: "80"
apiVersion: v1
kind: ConfigMap

metadata:
  name: {{ .Values.configMap.name }}

data:
{{- range $key, $value := .Values.configMap.data }}
  {{ $key }}: "{{ $value }}"
{{- end }}